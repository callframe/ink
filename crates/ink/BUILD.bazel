load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "ink",
    srcs = ["ink.rs"],
    edition = "2024",
    deps = [
        "@ink//crates/glfw-sys",
        "@crates//:mimalloc"
    ],
    visibility = ["@ink//:__pkg__"]
)
