load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "ink",
    srcs = ["src/ink.rs"],
    deps = [
        "@ink//crates/glfw-sys:glfw_sys",
        "@crates//:mimalloc"
    ],
    edition = "2024",
    visibility = ["@ink//:__pkg__"]
)
